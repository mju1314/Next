  - 把运行模式从 Next.js 后端 + Prisma 改成了静态前端 + 本地数据层。
  - 页面不再请求 /api/*，现在任务、目标、项目、Inbox、今日推荐、复盘、备份都走本地服务。
  - 旧 API 源码保留到了 src/app/_api，不再参与 Next 静态导出。
  - next.config.mjs 已改为静态导出。
  - 已加入 Capacitor Android 工程。
  - 已生成 out/ 并同步到 Android WebView。
  - 已成功构建 debug APK。

  当前数据存储说明：现在先用 WebView 的本地持久化存储实现，已经满足“不部署后端、APK 本机运行”。后续如果要更稳、更接近原
  生数据库，可以把 src/lib/client/local-data.ts 的底层替换成 Capacitor SQLite，页面层不用大改。

  验证结果：

  - npm run build：通过，成功生成静态 out/
  - npx tsc --noEmit --incremental false：通过
  - npm test：48 个测试全部通过
  - gradlew.bat assembleDebug：通过，APK 已生成

  以后重新打包可以这样执行：

  cd D:\Next
  npm run build
  npx cap sync android
  cd android
  $env:JAVA_HOME="D:\develop\JDK\jdk21"
  .\gradlew.bat assembleDebug

  输出仍在：

  android\app\build\outputs\apk\debug\app-debug.apk

